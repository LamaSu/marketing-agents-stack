# Research SOURCE inputs — kept local only (not redistributed): source video/audio +
# the verbatim ASR transcript. Our transformative summaries in research/*.md carry the substance.
research/source/
research/slides/**/*.jpg
research/slides/**/*.png

# Node
node_modules/
dist/
build/
*.tsbuildinfo
.turbo/

# Python
__pycache__/
*.pyc
.venv/
venv/

# ML model artifacts (trained offline; see packages/adapters-scoring/train/)
*.onnx

# Env / secrets
.env
.env.*
!.env.example
*.local

# State / DB
*.db
*.sqlite
chorus/state/
.data/

# Manim working dirs (regenerable; keep the .py + brief.md + final .mp4)
videos/**/media/
videos/**/_frames/
videos/**/*.render.log

# OS
.DS_Store
Thumbs.db

# SOTA competitive analysis — LOCAL ONLY, never pushed (user directive 2026-07-21)
research/sota/
research/10-sota-integration-design.md
# Build orchestration state + per-wave agent handoff notes — LOCAL ONLY (they
# reference the SOTA-adoption plan; keep local so a fresh session/subagent can
# resume with zero work lost, but never push the strategy).
research/BUILD-STATE.md
research/wave-*.md
