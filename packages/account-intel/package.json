{
  "name": "@mstack/account-intel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Account-Intelligence engine + agent swarm (context engine, scoring noise filter, SDR-Researcher/Copywriter/GTM-Router swarm, draft-first activateAccount).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@mstack/core": "workspace:*",
    "@mstack/agents": "workspace:*",
    "@mstack/memory": "workspace:*",
    "@mstack/adapters-signals": "workspace:*",
    "@mstack/adapters-enrichment": "workspace:*",
    "@mstack/adapters-scoring": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "rimraf": "^6.0.1"
  }
}
