{
  "name": "@mstack/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude-native agent mechanism (runAgent tool-use loop + Zod-validated structured output) — no LangChain.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@mstack/core": "workspace:*",
    "@anthropic-ai/sdk": "latest",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
